# MurSchol OS: compressed swap for smoother behaviour under memory pressure.
# The upstream generator recommends fractions in the 0.1–0.5 range and uses
# min(ram / 2, 4096) as its default. Keep that conservative default explicitly.
[zram0]
zram-size = min(ram / 2, 4096)
swap-priority = 100
